# Ahk2Exe でコンパイルした実行ファイルは追跡しない。
# 未署名・低流通の自作 exe はキーフックを含むため Microsoft Defender の
# 機械学習判定 (Wacatac.B!ml) で誤検知されやすく、しかも再コンパイルのたびに
# ハッシュが変わるので、ハッシュ単位の許可リストが毎回無効になる。
# 本ツールは .ahk を AutoHotkey64.exe で直接実行して常駐させる運用にしている。
*.exe
